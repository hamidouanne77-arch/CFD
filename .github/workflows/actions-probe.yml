name: CFD Actions probe

on:
  push:
    branches:
      - bootstrap/su2-v8.5.0-binary
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo PUBLIC_CFD_ACTIONS_OK
